{
  "name": "@zippie/guest-agent",
  "version": "0.1.0",
  "private": true,
  "description": "VCR Guest Agent with CMIO support for Cartesi Machine communication",
  "scripts": {
    "build": "docker build -t guest-agent .",
    "build-native": "cargo build --target riscv64gc-unknown-linux-musl --release",
    "test": "cargo test",
    "check": "cargo check"
  }
} 